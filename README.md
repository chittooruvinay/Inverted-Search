# Inverted-Search
Inverted Search Engine implemented in C using Data Structures. Builds an inverted index using hash tables and linked lists to map words to files with frequency counts. Supports create, update, search, display, and persistent storage operations.
